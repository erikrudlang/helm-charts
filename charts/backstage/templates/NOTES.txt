Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Values.namespace.name }}
  $ helm get all {{ .Release.Name }} -n {{ .Values.namespace.name }}

Backstage has been deployed to namespace: {{ .Values.namespace.name }}

1. Get the application URL by running these commands:
{{- if eq .Values.service.type "ClusterIP" }}
  export POD_NAME=$(kubectl get pods -n {{ .Values.namespace.name }} -l "app={{ include "backstage.name" . }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:7007 to use your application"
  kubectl port-forward $POD_NAME -n {{ .Values.namespace.name }} 7007:{{ .Values.service.targetPort }}
{{- end }}

2. Check the deployment status:
  kubectl get deployment {{ include "backstage.fullname" . }} -n {{ .Values.namespace.name }}

3. Check the PostgreSQL server status:
  kubectl get flexibleserver {{ .Values.postgresql.serverName }} -n {{ .Values.namespace.name }}

4. View the logs:
  kubectl logs -l app={{ include "backstage.name" . }} -n {{ .Values.namespace.name }} --tail=100 -f

{{- if .Values.postgresql.enabled }}

IMPORTANT: PostgreSQL Setup
- Server Name: {{ .Values.postgresql.serverName }}
- Admin User: {{ .Values.postgresql.administratorLogin }}
- Database: {{ .Values.postgresql.database.name }}
- FQDN: {{ include "backstage.postgresql.fqdn" . }}

Make sure to change the default administrator password in production!
{{- end }}

For more information, visit: https://backstage.io/docs
